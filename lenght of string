#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    printf("Enter a string: ");
    gets(str); // use fgets() for better safety
    printf("Length of the string is %d
", strlen(str));
    return 0;
}