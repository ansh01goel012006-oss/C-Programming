#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    printf("Enter a string: ");
    gets(str);
    strrev(str); // This works on Turbo C, for modern compilers use manual code
    printf("Reversed string: %s
", str);
    return 0;
}