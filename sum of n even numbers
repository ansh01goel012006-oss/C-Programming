#include <stdio.h>
int main() {
    int n, sum = 0, i, num = 2;
    printf("Enter n: ");
    scanf("%d", &n);
    for(i = 0; i < n; i++) {
        sum += num;
        num += 2;
    }
    printf("Sum = %d", sum);
    return 0;
}
