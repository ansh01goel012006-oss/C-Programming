#include <stdio.h>
int main() {
    float l, b, area, peri;
    printf("Enter length and breadth of rectangle: ");
    scanf("%f %f", &l, &b);

    area = l * b;
    peri = 2 * (l + b);

    printf("Area = %.2f, Perimeter = %.2f\n", area, peri);

}
