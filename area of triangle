#include <stdio.h>
int main() {
    float h, l, area;
    printf("Enter height and base of triangle: ");
    scanf("%f %f", &h, &l);

    area = (h * l) / 2;
    printf("Area of Triangle = %.2f\n", area);
    return 0;
}