#include <stdio.h>
#include <math.h>
int main() {
    int n, i, sum = 0;
    printf("Enter n: ");
    scanf("%d", &n);
    for(i = 1; i <= n; i++)
        sum += pow(9, i);
    printf("Sum = %d", sum);
   
}
